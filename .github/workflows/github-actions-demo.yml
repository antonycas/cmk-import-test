name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Messages
        run: |
          echo "Hello World"
          echo "Done - Goodbye"